<?xml version="1.0" encoding="UTF-8"?>
<tomee>
  <!-- see http://tomee.apache.org/containers-and-resources.html -->

  <!-- activate next line to be able to deploy applications in apps -->
  <!-- <Deployments dir="apps" /> -->
 <!-- <Resource id="asistencia-ds" type="DataSource">
        userName = root
        password = root
        jdbcDriver = com.mysql.jdbc.Driver
        jdbcUrl = jdbc:mysql://localhost/asistencia
        JtaManaged = true
        maxActive=50 
        maxIdle=50
        maxWait=-1
        InitialSize=1
        ValidationQuery="select 1"
   </Resource>
   <Resource id="asistencia-huella-ds" type="DataSource">
        userName = root
        password = root
        jdbcDriver = com.mysql.jdbc.Driver
        jdbcUrl = jdbc:mysql://localhost/asistencia
        JtaManaged = true
        maxActive=20 
        maxIdle=20
        maxWait=-1
        InitialSize=1
        ValidationQuery="select 1"
   </Resource>
 -->
</tomee>
